# 任务

- [x] 1.1 更新 README 和长期规格，说明 micro/small/standard 三种入口。
- [x] 1.2 更新 `skills/oz-plan`、`skills/oz-create`、`skills/oz-exec`、`skills/oz-archive`，让 small 使用 brief-only，standard 保持完整提案。
- [x] 1.3 更新 `oz validate`，允许 small 只包含 `brief.md`、`acceptance.json` 和 `tests/`。
- [x] 1.4 保持 small 的 `acceptance.json` 和 `tests/` 硬合同，不允许空测试或弱断言。
- [x] 1.5 运行本提案契约测试和相关 Go 回归。
