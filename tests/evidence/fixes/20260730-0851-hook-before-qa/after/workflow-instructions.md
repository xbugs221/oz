# 修复后工作流要求

自查任务现在明确把钩子稳定放在独立 QA 之前：

> 每次移交独立 QA 前，按仓库已有的显式入口运行提交前钩子，不得创建临时 commit；吸收改动后，再次运行不再修改文件才可移交。钩子稳定后重新运行受影响测试、全部 required tests 和 validation commands。

归档任务明确禁止首次触发改写：

> 不得在最终 QA 后首次触发会改写文件的提交前钩子；只允许复核上一自查阶段已确认幂等的同一入口。若仍产生改动，停止归档并返回自查与 QA，不得吸收为已测试内容。
