# 修复前工作流要求

修复前，自查任务直接从修复进入测试，没有要求提前运行提交前钩子：

> 每轮重新核对输入与验证；移交前复跑失败测试、全部 required tests 和 validation commands。

归档任务只禁止归档后的额外编辑，也没有规定钩子必须在最终 QA 前稳定：

> 必须从 state 封存的 delivery_base_head 新建且只新建一个完整交付 commit。
>
> 归档命令返回后，严禁编辑、格式化或恢复提案目录及任何源码。
