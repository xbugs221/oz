# 现在：按能否人工审核判断

## 使用同一类材料复核

验收使用了一段真实 AVI 视频，不再把它转换成 WebM。

## 实际看到什么

- 提案合同接受视频原有的 `.avi` 文件名。
- 完整归档检查能够读取该视频文件，不再报格式不受支持。
- 把五十行说明文字改成视频后缀时，系统仍明确提示“只是文本内容”，不会让它冒充演示。

## 对用户的改善

交付人员可以保留录制工具产出的格式；审核人员直接打开视频确认内容即可。
